/** Next.js router basePath — inlined by the bundler at build time. */
export const basePath =
  (typeof process !== 'undefined' &&
    (process.env as Record<string, string>).__NEXT_ROUTER_BASEPATH) ||
  '';
